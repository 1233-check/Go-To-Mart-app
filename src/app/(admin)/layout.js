export const metadata = {
    title: 'Admin Dashboard — Go To Mart',
    description: 'Manage products, orders, and delivery partners for Go To Mart',
};

export default function AdminLayout({ children }) {
    return <>{children}</>;
}
